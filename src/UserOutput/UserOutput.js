import React from 'react';

const userOutput = (props) => {
    return (
        <div>
            <p>{props.username}</p>
            <p>This is a sentance</p>
        </div>
    )
}

export default userOutput;